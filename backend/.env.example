# Variáveis de ambiente para o projeto Site ProsaAi

# Configurações do Banco de Dados
# Exemplo para PostgreSQL: postgresql://user:password@host:port/database_name
# Exemplo para SQLite (para desenvolvimento/testes): sqlite:///./sql_app.db
DATABASE_URL=postgresql://user:password@localhost:5433/prosaai

# Chave secreta para JWT (JSON Web Tokens)
# IMPORTANTE: Não use o valor abaixo em produção!
# Execute `python scripts/generate_secret.py` para gerar uma chave segura.
SECRET_KEY=your-super-secret-and-secure-key-here

# Chave da API do Google Gemini (Necessária para correção de redações com IA)
# Obtenha em: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_THEME_API_KEY=  # Opcional: API Key separada para geração de temas (se vazio, usa GEMINI_API_KEY)

# Chave da API do Groq (Necessária para correção de redações com Groq)
# Obtenha em: https://console.groq.com/keys
GROQ_API_KEY=your-groq-api-key-here
GROQ_THEME_API_KEY=  # Opcional: API Key separada do Groq para geração de temas (se vazio, usa GROQ_API_KEY)

# Algoritmo de criptografia para JWT (geralmente "HS256")
ALGORITHM=HS256

# Tempo de expiração do token de acesso em minutos
# Exemplo: 30 minutos
ACCESS_TOKEN_EXPIRE_MINUTES=240


# Tempo de expiração do token de acesso em dias
# Exemplo: 7 dias
REFRESH_TOKEN_EXPIRE_DAYS=7

# Origens permitidas para CORS (separadas por vírgula)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,http://127.0.0.1:3000

# URL do serviço de correção externa (se aplicável)
# Exemplo: http://localhost:8001/correct
CORRECTION_SERVICE_URL="http://localhost:8001/correct"

# Mercado Pago API Credentials
# Obtenha em: https://www.mercadopago.com.br/developers/panel/app
MP_PUBLIC_KEY=APP_USR-your-public-key-here
MP_ACCESS_TOKEN=APP_USR-your-access-token-here

# URLs da aplicação (para callbacks do Mercado Pago)
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:8000

# Variáveis para Docker Compose (se aplicável)
# Docker Compose aguarda o PostgreSQL estar pronto
# Pode ser configurado para esperar por um serviço específico
# PORTS_PG="5432:5432"
# DATABASE_HOST="db"

# Resend Email Service (para envio de emails)
# Obtenha em: https://resend.com/api-keys
RESEND_API_KEY=re_your-resend-api-key-here
FEEDBACK_EMAIL=feedback@corrigeai.online

# Stability AI (para geração de imagens com IA)
# Obtenha em: https://platform.stability.ai/account/keys
STABILITY_API_KEY=your-stability-api-key-here